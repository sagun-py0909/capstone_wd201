<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://getbootstrap.com/docs/5.3/assets/css/docs.css" rel="stylesheet">
    <title>Bootstrap Example</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .header {
            padding: 20px;
        }

        .body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .login-container {
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 8px;
            width: max-content;
            text-align: center;
            margin-top: -20px;
            transition: background-color 0.3s ease, color 0.3s ease;
            /* Add a smooth transition effect for color */
        }

        .login-container.hightlight-user {
            background-color: #ffffff;
            /* Change the color for user login */
            color: #333;
            /* Reset font color for user login */
        }

        .login-container.hightlight-creator {
            background-color: #2c2929;
            /* Change the color for creator login */
            color: white;
            /* Change font color for creator login */
        }

        .login-container h2 {
            color: inherit;
            /* Use the inherited color for h2 */
        }

        .login-creator {
            display: none;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #555;
        }

        .form-group input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        .form-group button {
            background-color: #007bff;
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .form-group button:hover {
            background-color: #0056b3;
        }
    </style>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Get references to the buttons and forms
            var box = document.getElementById('body')
            var userButton = document.getElementById('option1');
            var creatorButton = document.getElementById('option2');
            var userForm = document.querySelector('.login-user');
            var creatorForm = document.querySelector('.login-creator');
            var loginContainer = document.querySelector('.login-container');

            // Add click event listeners to the buttons
            userButton.addEventListener('click', function () {
                userForm.style.display = 'block';
                creatorForm.style.display = 'none';
                loginContainer.classList.remove('hightlight-creator');
                loginContainer.classList.add('hightlight-user');
            });

            creatorButton.addEventListener('click', function () {
                userForm.style.display = 'none';
                creatorForm.style.display = 'block';
                loginContainer.classList.remove('hightlight-user');
                loginContainer.classList.add('hightlight-creator');
            });
        });
    </script>
</head>

<body>
    <div class="header">
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">THE LMS</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/credits">Credits</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/authors">Top Authors</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                                aria-expanded="false">
                                Contact Us
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="#">Chat with us!</a></li>
                                <li><a class="dropdown-item" href="#"></a></li>
                                <li>
                                    <a class="dropdown-item" href="#">FAQ's</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </div>
    <div class="body" id="body">
        <div class="login-container">
            <input type="radio" class="btn-check" name="options" id="option1" autocomplete="off" checked>
            <label class="btn btn-secondary" for="option1">Learners</label>

            <input type="radio" class="btn-check" name="options" id="option2" autocomplete="off">
            <label class="btn btn-secondary" for="option2">Creators</label>
            <div class="login-user">
                <h2>Login as User</h2>
                <form action="/login" method="post">
                    <!-- /login/users -->
                    <div class="form-group">
                        <label for="username">Username:</label>
                        <input type="text" id="username" name="username" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Password:</label>
                        <input type="password" id="password" name="password" required>
                    </div>
                    <div class="form-group">
                        <button type="submit">Login</button>
                    </div>
                </form>
            </div>
            <div class="login-creator">
                <h2>Login as creator</h2>
                <form action="/login" method="post">
                    <!-- /login/creators -->
                    <div class="form-group">
                        <label for="username">Username:</label>
                        <input type="text" id="username" name="username" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Password:</label>
                        <input type="password" id="password" name="password" required>
                    </div>
                    <div class="form-group">
                        <button type="submit">Login</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>

</html>